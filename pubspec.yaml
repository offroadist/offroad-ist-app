name: offroad_ist
description: OffRoad.ist — Offroad Aksesuar Pazaryeri
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.4.0
  flutter_secure_storage: ^9.2.2
  cached_network_image: ^3.3.1
  flutter_rating_bar: ^4.0.1
  shared_preferences: ^2.3.0
  url_launcher: ^6.3.0
  intl: ^0.19.0
  shimmer: ^3.0.0
  badges: ^3.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
